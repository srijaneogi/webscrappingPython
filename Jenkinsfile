node {
    triggers {
    githubPush()
    }
    stage('Build') {
        echo "make"
    }
    stage('Test') { 
        echo "maketest"
    }
    stage('Deploy') { 
        echo "makedeploy"
    }
}
